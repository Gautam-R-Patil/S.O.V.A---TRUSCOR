/**
 * Deployment Scripts (4 scripts)
 *
 * launch.js            — Production app launcher with env validation
 * preflight.js         — Pre-build checks (Chromium, deps, configs)
 * warmup.js            — Browser warm-up and profile priming
 * download-chromium.js — Playwright Chromium binary fetcher
 *
 * @proprietary Script implementations omitted.
 */
